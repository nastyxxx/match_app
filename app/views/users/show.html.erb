<br />

<h1 class="font-weight-bolder font-italic"><%= @user.nickname%></h1>

<%= image_tag @user.image, class: "rounded-circle",  width: "200", height: "200"%>
<h3 class="font-weight-bolder font-italic">
  Introduction...
</h3>
<div class="d-block text-justify">
  <h5 class="font-weight-bolder font-italic">
    <%= @user.introduction%>
  </h5>
</div>
<h3 class="font-weight-bolder font-italic">
  Address...
</h3>
<div class="d-block text-justify">
  <h5 class="font-weight-bolder font-italic">
    <%= @user.address%>
  </h5>
</div>
<div id="display"></div>
<div id='map' class="rounded"></div>

<script>
  let map
  let geocoder
  const display = document.getElementById('display')

  // ユーザーのの地図
  function initMap(){
    geocoder = new google.maps.Geocoder()

    map = new google.maps.Map(document.getElementById('map'), {
      center: {lat: <%= @user.latitude %>, lng:<%= @user.longitude%>},
      zoom: 12,
    });

    marker = new google.maps.Marker({
      position:  {lat: <%= @user.latitude %>, lng:<%= @user.longitude%>},
      map: map
    });
  }
</script>